# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: member-crud.spec.ts >> leader can remove a member
- Location: e2e/member-crud.spec.ts:64:1

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: locator('ul[role="menu-bar"]')
Expected: visible
Timeout: 15000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 15000ms
  - waiting for locator('ul[role="menu-bar"]')

```

```yaml
- heading "Sign in" [level=2]
- text: Email
- textbox "Email": leader@example.com
- button "Send login link"
- alert: Too many login link requests. Please wait a few minutes and try again.
- paragraph:
  - button "Privacy notice"
```

# Test source

```ts
  1  | import { expect, type Page } from "@playwright/test";
  2  | import { BASE_URL } from "./config.ts";
  3  | 
  4  | // Drives the full magic-link flow: request a link for `email` and land on the
  5  | // authenticated desktop (menu bar visible). Outside production, the API returns
  6  | // a `devToken` alongside its response and LoginForm.tsx auto-verifies with it
  7  | // immediately (skipping the "login link has been sent" message) rather than
  8  | // requiring a human to go dig the link out of an email/console — see
  9  | // backend/src/application/auth/auth.use-cases.ts's requestMagicLink.
  10 | export async function loginAs(page: Page, email: string): Promise<void> {
  11 |   await page.goto(BASE_URL);
  12 |   await expect(page.locator("text=Sign in")).toBeVisible({ timeout: 15000 });
  13 |   await page.fill("#login-email", email);
  14 |   await page.click('button:has-text("Send login link")');
> 15 |   await expect(page.locator('ul[role="menu-bar"]')).toBeVisible({ timeout: 15000 });
     |                                                     ^ Error: expect(locator).toBeVisible() failed
  16 | }
  17 | 
  18 | export async function logout(page: Page): Promise<void> {
  19 |   await page.evaluate(() => localStorage.removeItem("authToken"));
  20 | }
  21 | 
```