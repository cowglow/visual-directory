DATABASE_URL=postgresql://app:app@localhost:5432/contact_book
JWT_SECRET=dev-secret-change-me
SPACE_JWT_SECRET=dev-space-secret-change-me
PORT=4000
CLIENT_ORIGIN=https://localhost:3000
SEED_LEADER_EMAIL=leader@example.com

# Only used once both are set (see src/infrastructure/mail/get-mailer.ts) — until then,
# magic links log to the console instead. Get a free API key at https://resend.com.
# EMAIL_FROM's domain must be verified with Resend first (see docs/HETZNER_DEPLOY.md).
RESEND_API_KEY=
EMAIL_FROM=Visual Directory <login@mail.yourdomain.com>
